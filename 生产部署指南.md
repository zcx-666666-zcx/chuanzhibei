# 非遗传承系统生产部署指南

本文档用于部署 `chuanzhiback/demo`（Spring Boot）与 `chuanzhifron`（微信小程序）到生产环境。

## 1. 部署前准备

- Java 17
- Maven 3.9+
- MySQL 8.x
- Redis 6.x+
- 反向代理（Nginx/网关，建议开启 HTTPS）

## 1.1 近期联调结论（2026-04-05）

- 当前版本已通过一轮小程序端联调，图片/视频与主要业务链路可运行。
- 生产与真机环境请优先使用 `https` 域名访问后端与静态资源。
- 3D 页面（`xrDemo3D`）功能链路可用，但展示效果仍建议在发布前替换为正式 `glb` 模型并复测材质/光照。

## 2. 后端关键环境变量

生产环境必须显式设置以下变量：

```bash
export SPRING_PROFILES_ACTIVE=prod

export DB_URL="jdbc:mysql://<mysql-host>:3306/intangible_heritage?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai"
export DB_USERNAME="<db-user>"
export DB_PASSWORD="<db-password>"

export REDIS_HOST="<redis-host>"
export REDIS_PORT="6379"

export SERVER_PORT="8001"
export PUBLIC_BASE_URL="https://<your-domain>"
# 须为具体来源，禁止仅配置 *；可与小程序、管理端域名并列，逗号分隔
export CORS_ALLOWED_ORIGIN_PATTERNS="https://servicewechat.com,https://<your-admin-if-any>"

# 长度不少于 32 字符，且不得使用开发默认值 dev-token-secret-change-in-prod
export TOKEN_SECRET="<至少32位的强随机密钥>"
export TOKEN_EXPIRE_SECONDS="2592000"

export INIT_SAMPLE_DATA="false"
export JPA_DDL_AUTO="validate"
export SEED_AR_PROJECT_TABLE="false"
export SCHEMA_BOOTSTRAP="false"
```

启动时 `prod` 下会执行 **ProductionEnvironmentValidator**：校验 `TOKEN_SECRET` 强度、`PUBLIC_BASE_URL` 为 `https`、`CORS_ALLOWED_ORIGIN_PATTERNS` 非空且不为 `*`。未通过则进程退出。

`application-prod.properties` 已默认：`spring.jpa.open-in-view=false`、`server.forward-headers-strategy=framework`（配合 Nginx 转发 `X-Forwarded-Proto` / `Host`）。

## 3. 数据库初始化建议

与 JPA 实体、小程序字段对齐的 **权威建表脚本** 为 `chuanzhiback/demo/src/main/resources/schema-bootstrap.sql`（`schema.sql` 仅作遗留说明，头部已注明请以 `schema-bootstrap.sql` 为准）。

首次初始化（空库）可选方式：

1. **手工**：对目标库执行 `schema-bootstrap.sql`（均为 `CREATE TABLE IF NOT EXISTS`，可重复执行）。
2. **随应用启动**：设置环境变量 `SCHEMA_BOOTSTRAP=true`，由 `SchemaBootstrapRunner` 在启动时执行 classpath 内的 `schema-bootstrap.sql`（生产若使用 `JPA_DDL_AUTO=validate`，仍需事先建好表）。

再按需执行 `data.sql`（仅用于演示/测试，**不建议生产导入演示数据**）。若已存在生产数据，不要重复执行 `data.sql`。

## 4. 反向代理（Nginx 示例）

在 `location /` 代理到 Spring Boot 时建议转发头信息，以便 `PUBLIC_BASE_URL` 为 https 时应用内生成的链接正确：

```nginx
proxy_set_header Host $host;
proxy_set_header X-Real-IP $remote_addr;
proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;
```

## 5. 后端构建与启动

```bash
cd chuanzhiback/demo
mvn -DskipTests clean package
java -jar target/demo-0.0.1-SNAPSHOT.jar
```

## 6. 健康检查（Actuator）

已启用 Actuator，生产默认仅暴露 `health` 与 `info`：

- `GET /actuator/health`
- `GET /actuator/health/liveness`
- `GET /actuator/health/readiness`
- `GET /actuator/info`

建议在负载均衡/容器编排中使用 `readiness` 作为就绪探针。

## 7. 小程序联调与发布要点

1. 编辑 `chuanzhifron/utils/env.js`：
   - 将 `APP_ENV` 改为 `'production'`。
   - 可选填写 `DEVELOPMENT_API_BASE_URL` 作为开发联调地址（推荐同域名 `https`，避免真机资源拦截）。
   - 填写 `PRODUCTION_API_BASE_URL` 为与后端一致的 **https 根地址**（无 `/api` 后缀、无尾部 `/`）；或在用户设备上首次运行前写入 `wx.setStorageSync('apiBaseUrl', 'https://...')`（后者优先级更高）。
   - 生产环境下 mock 会强制关闭，需走真实登录与接口。
2. 微信公众平台配置 **request / uploadFile / downloadFile** 合法域名，与上述根域名一致（须备案与 HTTPS）。
3. 开发者工具可临时关闭域名校验做联调；**真机与审核版** 必须依赖合法域名。
4. `project.config.json` 中已设置 `urlCheck: true`，避免本地误配掩盖域名问题。
5. 后端 CORS 与小程序、H5 管理端实际来源对齐；勿使用 `*`。

> 当前联调使用示例域名：`https://1151290dt34oy.vicp.fun`。发布时请替换为你的正式备案域名，并同步更新 `PUBLIC_BASE_URL` 与小程序合法域名白名单。

## 8. 发布后回归清单（最小集）

与 [`chuanzhifron/README.md`](chuanzhifron/README.md)「小程序功能说明」对应的主链路抽检：

- **账号**：登录/注册可用；个人中心资料编辑、头像上传成功。
- **首页**：通知与搜索资讯；轮播（`/banners`）与新闻详情；文化热点与 `newsList` 分页；精选非遗与非遗详情；推荐链 A～D；**寻迹导航八宫格**（新闻列表、国家/省级名录、传承人、技艺视频、活动、收藏、预约）均可进入且无白屏。**学习中心**不在首页入口；若需验收 `learning` 页须手动拼路径或临时加入口。
- **非遗详情**：收藏/取消与「我的收藏」列表一致。
- **活动与预约**：报名/预约写入与「我的预约」列表、取消预约一致。
- **沉浸 Tab**：3D 演示页可进入；体验项目列表与相机/视频体验页（如 `ARPlay`）可播放；体验历史可写入、可清理（与 `/api/ar/history` 等一致）。
- **后端**：`/actuator/health` 返回 `UP`。

## 9. 回滚策略

建议每次发布保留至少一个历史可运行包（jar）：

1. 停止当前进程
2. 使用上一版本 jar 启动
3. 验证 `/actuator/health` 与核心业务链路

若本次发布涉及数据库结构变更，需提前准备对应回滚 SQL（当前项目建议后续引入 Flyway 进行版本化迁移）。
