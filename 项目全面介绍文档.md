# 非遗传承系统项目全面介绍

本文档面向项目成员、指导老师和评审人员，系统说明本项目的结构、技术、功能、数据与运行方式。

## 1. 项目定位与目标

`非遗传承系统` 是一个前后端分离的数字文化应用，聚焦中国非物质文化遗产的展示、传播与互动体验。项目包含：

- 微信小程序端（用户访问入口）
- Spring Boot 后端（业务接口与数据管理）
- MySQL（业务数据）
- Redis（缓存与扩展能力）
- 本地静态资源目录（图片、视频等）

核心目标：

- 以数字化方式展示非遗项目、传承人、新闻与活动
- 提供收藏、预约、个人资料管理等用户闭环功能
- 提供 AR 项目浏览与体验记录能力
- 支持本地开发调试，并可平滑过渡到生产部署

---

## 2. 总体架构

整体采用前后端分离架构：

- 前端：微信小程序负责页面渲染、交互、路由、调用后端 API
- 后端：Spring Boot 提供 RESTful API、业务服务、数据持久化、静态资源映射
- 数据层：MySQL 存储结构化业务数据，Redis 作为缓存基础
- 文件层：上传文件和业务素材统一通过 `/uploads/**` 访问

调用链路：

1. 小程序页面发起请求（`utils/util.js`）
2. 统一拼接 API 地址（`utils/config.js`）
3. 后端 Controller 接收请求
4. Service 处理业务逻辑
5. Repository 操作数据库
6. 返回统一 `Result<T>` 响应

---

## 3. 目录结构（根目录）

- `chuanzhifron/`：微信小程序前端工程
- `chuanzhiback/demo/`：Spring Boot 后端工程
- `chuanzhiback/uploads/`：静态资源目录（图片、视频等）
- `README.md`：项目联调与启动说明
- `开发日志-2026-03-24.md`：阶段开发记录

---

## 4. 前端详细说明（`chuanzhifron`）

### 4.1 前端结构

- `app.js / app.json / app.wxss`：小程序入口、全局路由、全局样式
- `pages/`：业务页面
- `components/`：复用组件
- `utils/config.js`：联调地址与 URL 组装
- `utils/util.js`：统一请求层（token、超时、错误处理）
- `utils/auth.js`：登录注册、用户会话能力

### 4.2 主要页面功能

按 `tabBar` 五大模块组织：

1. 首页（`pages/index`）
   - 新闻展示、轮播图、推荐非遗、通知入口、搜索入口
2. 非遗名录（`pages/Heritage`）
   - 国家级/省级项目聚合与分类
3. AR体验（`pages/ARExperience`）
   - AR 项目列表、体验记录、跳转体验页
4. 传承人社区（`pages/InheritorCommunity`）
   - 传承人、技艺视频、活动与预约相关入口
5. 个人中心（`pages/PersonalCenter`）
   - 用户资料、头像上传、我的收藏、我的预约

扩展页面包含：

- 登录/注册：`pages/login`、`pages/register`
- 新闻列表/详情：`pages/newsList`、`pages/newsDetail`
- 非遗详情：`pages/heritageDetail`
- 国家级/省级清单页：`pages/nationalHeritageList`、`pages/provincialHeritageList`
- 传承人清单、视频清单、活动清单：`masterList`、`skillVideoList`、`activityList`
- 收藏/预约列表：`collectionList`、`bookingList`

### 4.3 前端工程策略

- 所有 API 请求走 `request()` 统一入口
- 静态资源 URL 通过 `buildStaticUrl()` 构建，避免硬编码
- 支持本地配置覆盖：
  - `wx.setStorageSync('apiBaseUrl', 'http://localhost:8001')`
- 请求层统一处理 401，保障登录态一致性

---

## 5. 后端详细说明（`chuanzhiback/demo`）

### 5.1 分层结构

- `controller/`：接口层
- `service/`：业务逻辑层
- `repository/`：数据访问层（JPA）
- `entity/`：实体模型
- `config/`：安全、CORS、静态资源映射等配置
- `common/Result.java`：统一响应结构

### 5.2 关键接口模块

- 认证与用户
  - `AuthController`、`UserController`
- 首页聚合
  - `HomeController`（首页数据、通知、debug 信息）
- 内容模块
  - `NewsController`、`HeritageController`、`BannerController`
- 社区模块
  - `InheritorController`、`VideoController`、`ActivityController`
- AR 模块
  - `ARProjectController`、`ArExperienceController`
- 用户行为
  - `UserCollectionController`、`UserBookingController`
- 文件上传
  - `FileUploadController`

### 5.3 统一返回结构

后端统一返回：

- `success: boolean`
- `message: string`
- `data: any`

前端据此统一解析，降低接口差异成本。

---

## 6. 技术栈

### 6.1 前端

- 微信小程序原生框架（WXML / WXSS / JS）
- 小程序原生 API（`wx.request`、`wx.uploadFile`、`wx.navigateTo` 等）

### 6.2 后端

- Java 17
- Spring Boot 3.5.8
- Spring Web
- Spring Data JPA
- Spring Security
- Spring Data Redis
- WebSocket 依赖（可扩展）

### 6.3 数据与构建

- MySQL 8（数据库：`intangible_heritage`）
- Redis
- Maven（`pom.xml`）

---

## 7. 核心数据模型（示例）

项目核心实体包括但不限于：

- `User`：用户信息
- `News`：新闻内容
- `Heritage`：非遗项目
- `Inheritor`：传承人
- `Video`：技艺视频
- `Activity`：活动信息
- `ARProject` / `ArExperience` / `ARExperienceRecord`：AR 项目与体验记录
- `UserCollection`：用户收藏
- `UserBooking`：用户预约
- `Banner`：首页轮播图

---

## 8. 运行与配置（本地为主）

### 8.1 后端

默认端口：`8001`

关键配置项：

- 数据库：`DB_URL` / `DB_USERNAME` / `DB_PASSWORD`
- Redis：`REDIS_HOST` / `REDIS_PORT`
- 服务端口：`SERVER_PORT`
- CORS：`CORS_ALLOWED_ORIGIN_PATTERNS`
- 静态资源目录：`UPLOAD_BASE_DIR`（默认 `../uploads`）
- 示例数据开关：`INIT_SAMPLE_DATA`（默认 `false`，避免覆盖真实库）

### 8.2 前端

- 微信开发者工具打开 `chuanzhifron`
- 默认后端地址：`http://localhost:8001`
- 可通过缓存覆盖 `apiBaseUrl`

---

## 9. 已实现功能清单

- 用户注册、登录、会话管理
- 首页新闻展示、搜索与通知展示
- 非遗项目列表、详情、收藏/取消收藏
- 传承人列表、技艺视频展示
- 活动报名/预约逻辑
- AR 项目浏览与体验记录
- 个人中心资料编辑、头像上传、收藏与预约管理
- 统一接口请求层与地址配置能力

---

## 10. 工程特点与可维护性设计

- 前后端分层清晰，边界明确
- 统一请求层与统一响应结构
- 静态资源访问可配置，便于本地/生产切换
- 路由冲突（字面路径与 ID 路径）已做规避（数字 ID 约束）
- 示例数据初始化可开关，避免污染真实业务数据

---

## 11. 后续建议

- 为关键接口补充自动化测试（登录、列表、详情、收藏、预约）
- 增加接口字段契约文档（OpenAPI/Swagger）
- 对图片资源增加后端兜底策略（缺图返回默认图）
- 增加联调健康检查页，一键验证接口和资源可用性

---

## 12. 总结

该项目已经具备完整的“内容展示 + 用户互动 + AR 扩展”能力，适合作为非遗数字化展示与实践型项目。当前在本地调试与真实数据库联调层面已具备较好基础，后续可通过测试体系、文档体系和部署规范进一步提升工程成熟度。
