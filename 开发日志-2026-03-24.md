# 开发日志（2026-03-24）

## 本次目标

- 面向“仅本地调试”场景，提升前后端联调稳定性与功能完整度。
- 减少小程序端占位逻辑，改为真实可用流程。
- 同步后端接口能力，避免前端功能无接口支撑。

## 完成内容

### 一、架构与配置

1. 前端新增统一配置模块 `chuanzhifron/utils/config.js`：
   - 统一 API/静态资源地址拼装。
   - 支持 `wx.setStorageSync('apiBaseUrl', 'http://IP:8001')` 动态切换联调地址。

2. 前端请求层 `chuanzhifron/utils/util.js`：
   - 统一请求入口、超时控制、401 清理登录态。

3. 后端配置外置化 `application.properties`：
   - 支持数据库、Redis、端口、CORS 环境变量注入。

4. 后端 CORS 动态化 `SecurityConfig`：
   - 支持逗号分隔来源白名单。

### 二、功能完善（前端）

1. 首页 `pages/index`：
   - 搜索按钮改为可输入关键词，并跳转新闻列表。
   - 通知按钮改为调用后端通知接口并展示最新通知。
   - 首页 Logo、轮播图、新闻图、推荐图统一使用动态静态地址组装。

2. 新闻列表 `pages/newsList`：
   - 支持 `keyword` 参数。
   - 自动调用 `/api/news/search` 或 `/api/news/recent`。

3. 新闻详情 `pages/newsDetail`：
   - 移除大段本地模拟数据分支，改为真实接口失败提示。
   - 图片 URL 统一走静态地址构造。

4. 个人中心 `pages/PersonalCenter`：
   - 设置项可实际切换本地隐私/通知开关（本地存储）。
   - 帮助与反馈、关于我们改为可展示真实信息。
   - 头像与上传相关 URL 统一为动态地址。

5. WXML 适配：
   - `index.wxml`、`register.wxml`、`PersonalCenter.wxml` 移除关键硬编码地址。

### 三、功能完善（后端）

1. 新闻搜索接口：
   - `GET /api/news/search?keyword=xxx`
   - 支持标题/描述/正文匹配。

2. 首页通知接口：
   - `GET /api/home/notifications`
   - 基于新闻聚合通知，满足本地演示与联调。

3. 联调状态接口：
   - `GET /api/home/debug-info`
   - 返回服务状态、时间戳及首页核心数据量。

4. 服务容错优化：
   - `NewsService.searchByKeyword` 增加数据库查询失败回退（内存过滤），保障本地可用性。

### 四、代码质量

- 本次修改文件已通过 IDE linter 检查，无新增 lint 错误。
- 清理了后端若干 `printStackTrace` 与未使用导入告警。

## 当前已知说明

- 机器环境缺少 Java Runtime，无法在当前环境执行 `./mvnw compile`。
- 项目仍有部分页面保留示例数据兜底（用于本地展示连续性），但关键链路（登录、首页、新闻、个人中心、上传）已打通并可联调。

## 下一步建议

1. 在本机安装 JDK 17 后，执行后端编译与启动校验：
   - `cd chuanzhiback/demo && ./mvnw -DskipTests compile`
   - `./mvnw spring-boot:run`
2. 微信开发者工具真机调试时，执行：
   - `wx.setStorageSync('apiBaseUrl', 'http://你的局域网IP:8001')`
3. 后续可继续把其余页面残留 `localhost` 字符串全部迁移到 `buildStaticUrl`，实现全项目统一。

---

## 二次迭代补充（同日）

### 背景

用户反馈：小程序多个页面图片与文字加载不出来，希望按真实生产环境方式修复。

### 本次修复项

1. 全面清理页面硬编码资源地址：
   - AR 页面（`ARExperience`、`ARProjectList`、`ARPlay`）
   - 非遗页面（`Heritage`、`nationalHeritageList`、`provincialHeritageList`、`heritageDetail`）
   - 社区页面（`InheritorCommunity`、`masterList`、`skillVideoList`）
   - 收藏页（`collectionList`）

2. 统一改为 `buildStaticUrl(...)` 动态拼接，适配本地调试 / 局域网 IP / 生产域名切换。

3. AR 相关 WXML 默认图片改为数据绑定回退，避免静态模板中写死地址导致的失效。

4. 关键结果：
   - 前端代码中已无 `http://localhost:8001` 业务硬编码（仅文档说明保留）。
   - 本轮修改文件 lint 通过，无新增语法与规范告警。
