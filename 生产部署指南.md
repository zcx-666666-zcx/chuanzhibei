# 非遗传承系统生产部署指南

本文档用于部署 `chuanzhiback/demo`（Spring Boot）与 `chuanzhifron`（微信小程序）到生产环境。

## 1. 部署前准备

- Java 17
- Maven 3.9+
- MySQL 8.x
- Redis 6.x+
- 反向代理（Nginx/网关，建议开启 HTTPS）

## 2. 后端关键环境变量

生产环境必须显式设置以下变量：

```bash
export SPRING_PROFILES_ACTIVE=prod

export DB_URL="jdbc:mysql://<mysql-host>:3306/intangible_heritage?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai"
export DB_USERNAME="<db-user>"
export DB_PASSWORD="<db-password>"

export REDIS_HOST="<redis-host>"
export REDIS_PORT="6379"

export SERVER_PORT="8001"
export PUBLIC_BASE_URL="https://<your-domain>"
export CORS_ALLOWED_ORIGIN_PATTERNS="https://<your-miniapp-domain>"

export TOKEN_SECRET="<strong-random-secret>"
export TOKEN_EXPIRE_SECONDS="2592000"

export INIT_SAMPLE_DATA="false"
export JPA_DDL_AUTO="validate"
```

## 3. 数据库初始化建议

首次初始化（空库）建议顺序：

1. 先执行 `schema.sql`
2. 再按需执行 `data.sql`（仅用于演示/测试，不建议生产导入演示数据）

如果已存在生产数据，不要重复执行 `data.sql`。

## 4. 后端构建与启动

```bash
cd chuanzhiback/demo
mvn -DskipTests clean package
java -jar target/demo-0.0.1-SNAPSHOT.jar
```

## 5. 健康检查（Actuator）

已启用 Actuator，生产默认仅暴露 `health` 与 `info`：

- `GET /actuator/health`
- `GET /actuator/health/liveness`
- `GET /actuator/health/readiness`
- `GET /actuator/info`

建议在负载均衡/容器编排中使用 `readiness` 作为就绪探针。

## 6. 小程序联调与发布要点

- 小程序请求地址请使用可访问的生产域名，不要使用 `localhost`。
- 后端 CORS 必须与实际来源一致（`CORS_ALLOWED_ORIGIN_PATTERNS`）。
- 发布前确认登录、收藏、预约、AR 记录写入链路均使用生产地址。

## 7. 发布后回归清单（最小集）

- 登录/注册可用
- 首页、新闻、非遗列表可正常加载
- 收藏新增/取消与“我的收藏”一致
- 预约新增/取消与“我的预约”一致
- AR 体验历史可写入、可清理
- `/actuator/health` 返回 `UP`

## 8. 回滚策略

建议每次发布保留至少一个历史可运行包（jar）：

1. 停止当前进程
2. 使用上一版本 jar 启动
3. 验证 `/actuator/health` 与核心业务链路

若本次发布涉及数据库结构变更，需提前准备对应回滚 SQL（当前项目建议后续引入 Flyway 进行版本化迁移）。
