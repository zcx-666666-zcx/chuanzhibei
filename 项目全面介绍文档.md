# 非遗传承系统项目全面介绍

本文档面向项目成员、指导老师和评审人员，系统说明本项目的结构、技术、功能、数据与运行方式。

## 1. 项目定位与目标

`非遗传承系统` 是一个前后端分离的数字文化应用，聚焦中国非物质文化遗产的展示、传播与互动体验。项目包含：

- 微信小程序端（用户访问入口）
- Spring Boot 后端（业务接口与数据管理）
- MySQL（业务数据）
- Redis（缓存与扩展能力）
- 本地静态资源目录（图片、视频等）

核心目标：

- 以数字化方式展示非遗项目、传承人、新闻与活动
- 提供收藏、预约、个人资料管理等用户闭环功能
- 提供 3D/沉浸体验记录能力
- 支持本地开发调试，并可平滑过渡到生产部署

---

## 2. 总体架构

整体采用前后端分离架构：

- 前端：微信小程序负责页面渲染、交互、路由、调用后端 API
- 后端：Spring Boot 提供 RESTful API、业务服务、数据持久化、静态资源映射
- 数据层：MySQL 存储结构化业务数据，Redis 作为缓存基础
- 文件层：上传文件和业务素材统一通过 `/uploads/**` 访问

调用链路：

1. 小程序页面发起请求（`utils/util.js`）
2. 统一拼接 API 地址（`utils/config.js`）
3. 后端 Controller 接收请求
4. Service 处理业务逻辑
5. Repository 操作数据库
6. 返回统一 `Result<T>` 响应

---

## 3. 目录结构（根目录）

- `chuanzhifron/`：微信小程序前端工程
- `chuanzhiback/demo/`：Spring Boot 后端工程
- `chuanzhiback/uploads/`：静态资源目录（图片、视频等）
- `README.md`：项目联调与启动说明
- `生产部署指南.md`：生产环境变量、建库、Nginx、小程序发布与回归清单
- `开发日志-2026-03-24.md`：阶段开发记录
- `chuanzhifron/README.md`：**小程序功能说明**（与 `app.json`、首页 WXML 一致）
- `竞赛迭代规划与实现路径.md`：功能树、HTTP 约定、分阶段任务与验收勾选

---

## 4. 前端详细说明（`chuanzhifron`）

### 4.1 前端结构

- `app.js / app.json / app.wxss`：入口；**冷启动无 token 时 `redirectTo` 登录**；`globalData.localDataMode` 与 `utils/env.js` 的 `useMockApi()` 一致
- `pages/`：业务页面（见下节「`app.json` 页面注册表」）
- `components/xr-demo-viewer/`：3D 演示（`xrDemo3D` 引用）
- `components/news-card/`：**当前未在任意页面 `usingComponents` 中引用**（`newsList` 使用内联结构），属遗留组件
- `utils/env.js`：`APP_ENV`、`USE_MOCK_IN_DEVELOPMENT`、开发/生产 API 根地址
- `utils/config.js`：`getBaseUrl`、`buildApiUrl`、`buildStaticUrl`，`API_PREFIX=/api`；支持 `wx.setStorageSync('apiBaseUrl')` 覆盖
- `utils/util.js`：`request`、`requestErrorMessage`、401/403 行为、`formatTime`
- `utils/auth.js`：登录注册；**登出仅清本地 Storage**，不调 `POST /auth/logout`
- `utils/newsDisplay.js`：新闻分页响应解析与列表项映射
- `utils/resourceProfile.js`：视频预加载、3D 页网络与 DPR 策略
- `utils/xrModels.js`：模型清单兜底（配合 `GET /ar/models`）
- `utils/mockApi.js`：本地假数据（`useMockApi()` 为 true 时生效）

### 4.2 主要页面功能

按 `tabBar` **三个主入口**组织（与 `app.json` 一致）。**逐块产品说明**以 `chuanzhifron/README.md`「小程序功能说明」为准，此处为摘要：

1. **首页**（`pages/index`）
   - **遗韵顶区**：新闻关键词搜索、首页通知（`GET /home/notifications`）。
   - **寻迹导航（八宫格）**：新闻列表与详情；国家/省级名录与非遗详情；传承人列表与详情、列表内预约体验；技艺视频；活动报名/预约观看；我的收藏；我的预约。**学习中心不在首页九宫格**（`learning` 页仍注册在 `app.json`，无 `navigateTo` 入口）。
   - **推荐跳转链**：链 A～D（新闻流、名录聚合、示例详情、切换沉浸 Tab）。
   - **轮播 / 文化热点 / 精选非遗**：轮播新闻进详情；热点按点击量展示并可进入全列表；精选 4～5 个代表性非遗进详情。
   - **沉浸通栏**：切换至沉浸 Tab。

2. **沉浸**（`pages/ARExperience`）
   - 静态引导区块；**3D 沉浸演示**（`pages/xrDemo3D`，`components/xr-demo-viewer`）；**横向 3D/沉浸体验项目**（数据来自 `GET /ar/projects` 等，可进 `ARPlay`）；**体验记录**（`GET /ar/history`）。

3. **个人中心**（`pages/PersonalCenter`）
   - 头像、昵称、签名与收藏/预约统计；编辑资料（含上传）；最近收藏与最近预约摘要。

**非 Tab 但常用的二级页**：

- 非遗名录：`pages/Heritage`（国家级/省级聚合与分类）
- 登录/注册：`pages/login`、`pages/register`
- 新闻列表/详情：`pages/newsList`、`pages/newsDetail`
- 非遗详情：`pages/heritageDetail`
- 国家级/省级清单页：`pages/nationalHeritageList`、`pages/provincialHeritageList`
- 传承人清单、视频清单、活动清单：`masterList`、`skillVideoList`、`activityList`
- AR 全量列表与相机页：`ARProjectList`、`ARPlay`
- 学习中心（引导）：`pages/learning`
- 收藏/预约列表：`collectionList`、`bookingList`

#### `app.json` 页面注册表（与代码一致）

按注册顺序：`index`，`login`，`register`，`Heritage`，`ARExperience`，`xrDemo3D`，`PersonalCenter`，`logs`，`learning`，`newsDetail`，`heritageDetail`，`ARPlay`，`newsList`，`nationalHeritageList`，`provincialHeritageList`，`masterList`，`inheritorDetail`，`skillVideoList`，`activityList`，`ARProjectList`，`collectionList`，`bookingList`。底部 Tab 仅为 **首页 / 沉浸 / 我的**。

**未注册占位目录**（不参与小程序打包路由）：`pages/Admin/`、`pages/bannerManagement/`。

### 4.3 前端工程策略

- 所有 API 请求走 `request()` 统一入口
- 静态资源 URL 通过 `buildStaticUrl()` 构建，避免硬编码
- 支持本地配置覆盖：
  - `wx.setStorageSync('apiBaseUrl', 'http://localhost:8001')`
- 请求层统一处理 401，保障登录态一致性

---

## 5. 后端详细说明（`chuanzhiback/demo`）

### 5.1 分层结构

- `controller/`：接口层
- `service/`：业务逻辑层
- `repository/`：数据访问层（JPA）
- `entity/`：实体模型
- `config/`：安全、CORS、静态资源映射等配置
- `common/Result.java`：统一响应结构

### 5.2 关键接口模块

- 认证与用户：`AuthController`、`UserController`（小程序常用 `POST /auth/login|register`、`PUT /users/{id}`；**未调用** `POST /auth/logout`）
- 首页与运营：`HomeController`（小程序使用 **`GET /home/notifications`**；`/home/data`、`/home/debug-info` 为可选/联调）
- 内容：`NewsController`、`HeritageController`、`BannerController`（小程序以只读列表/详情为主；各 Controller 内 **管理类 POST/PUT/DELETE** 多为扩展能力）
- 传承人 / 视频 / 活动：`InheritorController`、`VideoController`、`ActivityController`
- **学习中心**：`LearningController`（`/api/learning/path`，`GET/POST /learning/progress`），与 `pages/learning/learning` 对应；**当前无首页入口**
- **AR 主链路**：`ARProjectController`（`/api/ar/projects`、`/ar/models`、体验历史等）
- **遗留并行模块**：`ArExperienceController`（`/api/ar-experiences/**`，旧 `ArExperience` 实体）；前台沉浸已以 `ARProject` + `ARExperienceRecord` 为主，**小程序不调用该前缀**
- **已下线社区 API**：`InheritorCommunityController`（`/api/inheritor-community/**`），**小程序不调用**
- 用户行为：`UserCollectionController`、`UserBookingController`
- 文件：`FileUploadController`（如 `POST /files/upload-image`）

### 5.3 启动与数据准备（`config`）

- `SchemaBootstrapRunner`：可选执行 `schema-bootstrap.sql`（由 `SCHEMA_BOOTSTRAP` 等控制，见 `application-*.properties`）
- `ArProjectTableSeedRunner`：`SEED_AR_PROJECT_TABLE` 控制是否种子化 AR 项目表
- `ProductionEnvironmentValidator`：`prod` 下校验 `TOKEN_SECRET`、`PUBLIC_BASE_URL`（https）、CORS 等

### 5.4 统一返回结构

后端统一返回：

- `success: boolean`
- `message: string`
- `data: any`

前端据此统一解析，降低接口差异成本。

---

## 6. 技术栈

### 6.1 前端

- 微信小程序原生框架（WXML / WXSS / JS）
- 小程序原生 API（`wx.request`、`wx.uploadFile`、`wx.navigateTo` 等）

### 6.2 后端

- Java 17
- Spring Boot 3.5.8
- Spring Web
- Spring Data JPA
- Spring Security
- Spring Data Redis
- WebSocket 依赖（可扩展）

### 6.3 数据与构建

- MySQL 8（数据库：`intangible_heritage`）
- Redis
- Maven（`pom.xml`）

---

## 7. 核心数据模型（示例）

项目核心实体包括但不限于：

- `User`：用户信息
- `News`：新闻内容
- `Heritage`：非遗项目
- `Inheritor`：传承人
- `Video`：技艺视频
- `Activity`：活动信息
- `ARProject` + `ARExperienceRecord`：当前小程序沉浸/体验/历史主模型
- `ArExperience`：旧版 REST `/api/ar-experiences` 对应实体，与 `ARProject` 并存于库内，**小程序不直连该接口**
- `UserCollection`：用户收藏
- `UserBooking`：用户预约
- `Banner`：首页轮播图

---

## 8. 运行与配置（本地为主）

### 8.1 后端

默认端口：`8001`

关键配置项：

- 数据库：`DB_URL` / `DB_USERNAME` / `DB_PASSWORD`
- Redis：`REDIS_HOST` / `REDIS_PORT`
- 服务端口：`SERVER_PORT`
- CORS：`CORS_ALLOWED_ORIGIN_PATTERNS`
- 静态资源目录：`UPLOAD_BASE_DIR`（默认 `../uploads`）
- 开发环境（`application-dev.properties`）：`INIT_SAMPLE_DATA`、`SEED_AR_PROJECT_TABLE`、`SCHEMA_BOOTSTRAP` 等默认多为 `true`，便于本地一键跑通；**生产环境（`application-prod.properties`）对应项默认为 `false`**，详见 `生产部署指南.md`

### 8.2 前端

- 微信开发者工具打开 `chuanzhifron`
- 默认后端地址：`http://localhost:8001`
- 可通过缓存覆盖 `apiBaseUrl`

---

## 9. 已实现功能清单

- 用户注册、登录、本地会话（401 统一处理）
- 首页：Banner、新闻热点、精选非遗、通知、八宫格导航、推荐链 A～D
- 非遗名录与详情、国家/省级列表筛选、收藏/取消收藏
- 传承人列表与详情页、列表内预约体验；技艺视频列表；活动列表与报名/预约观看
- 沉浸 Tab：3D 演示（`xrDemo3D` + `xr-demo-viewer`，`GET /ar/models`）、体验项目横滑与全量列表、`ARPlay` 与体验历史读写/清理
- 个人中心：资料编辑、头像上传、收藏与预约摘要及列表、取消预约
- 统一请求层、`buildStaticUrl`、环境开关（`utils/env.js`）
- **学习中心**：后端 `LearningController` 与 `pages/learning` 页面已实现，**当前无首页入口**，不作为 Demo 必演示项

---

## 10. 工程特点与可维护性设计

- 前后端分层清晰，边界明确
- 统一请求层与统一响应结构
- 静态资源访问可配置，便于本地/生产切换
- 路由冲突（字面路径与 ID 路径）已做规避（数字 ID 约束）
- 示例数据初始化可开关，避免污染真实业务数据

---

## 11. 后续建议

- 为关键接口补充自动化测试（登录、列表、详情、收藏、预约）
- 增加接口字段契约文档（OpenAPI/Swagger）
- 对图片资源增加后端兜底策略（缺图返回默认图）
- 增加联调健康检查页，一键验证接口和资源可用性

---

## 11.1 当前迭代进度（2026-04-05）

与 `竞赛迭代规划与实现路径.md` v4.8 及本仓库代码同步；本轮已完成并勾选：

- [x] 阶段 0：`0.1` 配置文档化、`0.2` 外链替换为本地 `uploads`、`0.3` `app.json` 页面与 Tab 对齐、`0.4` 健康检查与环境变量约定
- [x] 阶段 1：`1.4` AR 主链路接口化（`/ar/projects`、`/ar/projects/{id}`、历史写入/读取/清理）与页面路径对齐
- [x] 阶段 1：`1.b` 后端稳定性收敛（收藏/预约删除命中校验、ID 入参校验、AR 分页字段兼容）
- [x] 阶段 2：`2.4` 弱网/权限/无视频地址降级（失败重试、权限提示、无 `videoUrl` 文案）
- [x] 阶段 2：`2.5` 资源加载优化（图片懒加载、视频按网络预加载、3D 页面按需加载与慢网分辨率降档）
- [x] 阶段 3：`3.C` 自有模型接入（`/api/ar/models` + `xrDemo3D` 模型选择器，后端清单优先、本地 uploads 兜底）
- [x] 联调验证：图片/视频在 HTTPS 域名下可用，收藏状态 403 与静态资源映射问题已收敛
- [ ] 3D 视觉增强：当前 3D 链路可运行，但高质量模型与最终效果仍待后续补充
- [x] 已知差异：导航方式、沉浸主路径、`videoUrl` 优先级、活动报名与预约串联、新闻页重试链路已对齐
- [ ] 待推进：阶段 3 差异化能力（`3.A/3.B` 择 1 深挖）

---

## 12. 总结

该项目已经具备完整的「内容展示 + 用户互动 + 3D/沉浸与 AR 体验扩展」能力，适合作为非遗数字化展示与实践型项目。界面级功能说明见 `chuanzhifron/README.md`。当前在本地调试与真实数据库联调层面已具备较好基础，后续可通过测试体系、文档体系和部署规范进一步提升工程成熟度。
